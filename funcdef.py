def hello(to="World"):
    print("Hello",to)

name =input("What's your name? ")

#we are gonna create our own function named hello using def for define
#using "to", so that we can pass the parameter "name". name copied to "to"
#you can give default values to the parameters. to="world" just in case we user does not enter.

hello(name)

